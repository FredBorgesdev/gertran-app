import { SimpleHourPipe } from './simple-hour.pipe';

describe('SimpleHourPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleHourPipe();
    expect(pipe).toBeTruthy();
  });
});
